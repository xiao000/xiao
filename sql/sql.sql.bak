SET NAMES 'utf8';
DROP DATABASE IF EXISTS color;
CREATE DATABASE color CHARSET=UTF8;
USE color;
CREATE TABLE c_color(
	colorid INT PRIMARY KEY AUTO_INCREMENT,
	c VARCHAR(64),
	m VARCHAR(64),
	y VARCHAR(64),
	k VARCHAR(64)
);
INSERT INTO c_color VALUES()